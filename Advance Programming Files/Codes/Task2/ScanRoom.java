// The ScanRoom class represents a specialized room in the medical institution where scans are conducted.
// It inherits properties from the SurgeryRoom.
public class ScanRoom extends SurgeryRoom {

    // Constructor
    public ScanRoom(int roomNumber) {
        super(roomNumber); // Calls the constructor of the parent class (SurgeryRoom)
    }

    // As there are no additional attributes or methods in ScanRoom as per your UML, 
    // the ScanRoom class primarily inherits everything from SurgeryRoom.
    // If needed, additional methods or attributes specific to ScanRoom can be added later.
}
